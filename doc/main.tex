\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{parskip}

\geometry{
    left=2.5cm,
    right=2.5cm,
    top=2.5cm,
    bottom=2.5cm
}
\onehalfspacing
\raggedbottom

\usepackage{graphicx}
\usepackage{float}
\usepackage{subcaption}
\graphicspath{{./}{./assets/}{./images/}}

\newcommand{\testimg}[1]{%
    \IfFileExists{assets/#1}{%
        \includegraphics[width=\textwidth]{assets/#1}%
    }{%
        \fbox{\parbox[c][4cm][c]{0.95\textwidth}{\centering\itshape\small (placeholder: assets/#1)}}%
    }%
}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.misc, arrows.meta, positioning, calc, automata, quotes, trees, fit, backgrounds, matrix, chains, decorations.pathreplacing}

\usepackage{listings}
\usepackage{xcolor}

\definecolor{codebg}{HTML}{F7F7F7}
\definecolor{codeframe}{HTML}{DDDDDD}
\definecolor{keyword}{HTML}{005FAF}
\definecolor{comment}{HTML}{6A737D}
\definecolor{string}{HTML}{D73A49}
\definecolor{accent}{HTML}{2E7D32}
\definecolor{warn}{HTML}{C62828}

\lstset{
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{keyword}\bfseries,
    commentstyle=\color{comment}\itshape,
    stringstyle=\color{string},
    numbers=left,
    numberstyle=\tiny\color{gray},
    backgroundcolor=\color{codebg},
    frame=single,
    rulecolor=\color{codeframe},
    breaklines=true,
    tabsize=4,
    showstringspaces=false,
    language=C++,
    aboveskip=1em,
    belowskip=1em,
    extendedchars=true,
    inputencoding=utf8,
    literate=%
        {→}{{$\rightarrow$}}1
        {á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'o}}1 {ú}{{\'u}}1
}

\usepackage{amsmath}
\usepackage{amssymb}

\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{etoolbox}
\usepackage{fancyvrb}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=keyword,
    citecolor=black,
    pdfauthor={Made Branenda Jordhy, Stefani Angeline Oroh, Michael James Liman},
    pdftitle={IF2211 Algorithm Strategy – Laporan Tugas Besar 3}
}

% HEADER/FOOTER
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\setlength{\headheight}{36pt}
\fancyhead[L]{%
    \small\textnormal{School of Electrical Engineering and Informatics}\\[-1pt]
    \small\textnormal{Institut Teknologi Bandung}%
}
\fancyhead[R]{\IfFileExists{assets/LOGO.png}{\includegraphics[height=1cm]{assets/LOGO.png}}{}}
\fancyfoot[L]{\small\textnormal{IF2211 Algorithm Strategy}}
\fancyfoot[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.2pt}

% TITLE
\title{
    \vspace{-1.5cm}
    {\Large\textsc{IF2211 Algorithm Strategy}}\\[0.35cm]
    {\LARGE\textbf{Pattern Matching in Chromium Browser Extension for Online Gambling Detection}}\\[0.3cm]
    \vspace{-0.3cm}
}
\author{}
\date{\today}

\begin{document}

\maketitle
\thispagestyle{empty}

\begin{figure}[H]
    \centering
    \IfFileExists{assets/Team_cropped.png}{% 
        \includegraphics[width=\textwidth,height=0.2\textheight,keepaspectratio]{assets/Team_cropped.png}%
    }{%
        \vspace{4cm}
    }
\end{figure}

\vspace{1cm}
\begin{center}
    \textbf{Made Branenda Jordhy} - 13524026 \\[0.15cm]
    \textbf{Stefani Angeline Oroh} - 13524064 \\[0.15cm]
    \textbf{Michael James Liman} - 13524106 \\[0.15cm]
\end{center}

\vspace{3cm}
\begin{center}
    {\large\textbf{School of Electrical Engineering and Informatics}}\\[0.1cm]
    {\large\textbf{Institut Teknologi Bandung}}\\[0.1cm]
    {\large\textbf{Semester II 2025/2026}}\\[0.1cm]
\end{center}

\newpage
\tableofcontents
\newpage

% =======================================================================
\newpage
\section{Overview}

Proyek ini kami kerjakan untuk memenuhi Tugas Besar 3 mata kuliah IF2211 Strategi Algoritma. Topiknya sudah ditentukan,
yaitu membuat sebuah \textit{Chromium browser extension} yang bisa mendeteksi konten judi online di halaman web, sekaligus
jadi tempat menerapkan algoritma pencocokan string yang dipelajari di kelas.

Topik judol sendiri sebetulnya relevan jika dilihat dari kondisi sekarang. Iklannya ada di mana-mana, mulai dari hasil pencarian
Google, kolom komentar YouTube, sampai sisipan iklan di situs yang sebenarnya tidak ada hubungannya sama sekali dengan perjudian.
Nama-nama yang dipakai pun mirip semua, biasanya berpola \texttt{<kata><angka>} seperti GACOR99, MAXWIN88, atau MADU308, supaya 
gampang diingat dan susah disaring otomatis. Tidak jarang pelakunya juga menyamarkan tulisan dengan karakter yang bentuknya mirip,
misalnya huruf O jadi angka 0, A jadi 4, atau I jadi 1, sehingga filter yang cuma membandingkan string mentah sering kebobolan.

Ekstensi yang kami bangun untuk tugas ini kami beri nama \textbf{Judol Detector}. Begitu halaman selesai dimuat, ekstensi akan
menelusuri DOM, mencocokkan teks dengan daftar kata kunci, lalu menandai elemen yang mengandung kemunculan kata kunci tersebut.
Pengguna bisa mengarahkan kursor ke teks yang ditandai untuk melihat detailnya, atau membuka panel popup ekstensi untuk melihat
ringkasan statistik secara keseluruhan. Pencocokan kata kunci dijalankan dengan beberapa algoritma sekaligus, yaitu Knuth-Morris-Pratt
dan Boyer-Moore untuk tahap pencocokan eksak, RegEx untuk menangkap pola \texttt{<kata><angka>} yang belum tentu ada di daftar kata kunci,
lalu Weighted Levenshtein Distance untuk tahap fuzzy yang menangani kasus penyamaran karakter. Seluruh algoritma string matching
dibuat \textit{from scratch}, tanpa bantuan \texttt{String.includes()}, \texttt{String.indexOf()}, atau pustaka pencocokan string apapun.
Tiap algoritma juga menghitung jumlah perbandingan karakter (\textit{comparison counting}) supaya performanya bisa dibandingkan secara
objektif.

% =======================================================================
\newpage
\section{Theoretical Background}

\subsection{Algoritma Knuth-Morris-Pratt (KMP)}
KMP pada dasarnya merupakan satu observasi sederhana pada matching brute-force. Jika pencarian gagal di tengah jalan, sebetulnya kita
sudah punya cukup informasi dari karakter yang sudah dibandingkan untuk tahu sejauh mana pola harus digeser. Brute-force tidak
peduli soal itu dan mengembalikan pointer teks ke posisi sebelumnya, jadi banyak perbandingan yang sudah dilakukan terbuang sia-sia.
KMP memanfaatkan informasi tadi supaya pointer teks bisa terus maju. Hasilnya, kompleksitas pencariannya $O(n+m)$ untuk teks
berukuran $n$ dan pola berukuran $m$.

Kunci algoritma ini adalah \textit{border function}, yaitu tabel yang menyimpan
panjang \textit{proper prefix} terpanjang pada pola yang sekaligus juga merupakan suffix-nya, sampai posisi tertentu. Tabel ini
dihitung sekali di awal dengan kompleksitas $O(m)$. Kalau mismatch terjadi di indeks $i$ pada pola, pointer pola dipindahkan ke
posisi $b[i-1]$, sementara pointer teks tetap di tempatnya. Contoh nilai border function untuk pola \texttt{ababaca} bisa dilihat
di Tabel~\ref{tab:kmp-border}.

\begin{table}[H]
    \centering
    \caption{Border function untuk pola \texttt{ababaca}.}
    \label{tab:kmp-border}
    \begin{tabular}{c|ccccccc}
        \toprule
        $i$    & 0 & 1 & 2 & 3 & 4 & 5 & 6 \\
        $P[i]$ & a & b & a & b & a & c & a \\
        $b[i]$ & 0 & 0 & 1 & 2 & 3 & 0 & 1 \\
        \bottomrule
    \end{tabular}
\end{table}

Kompleksitas pencariannya sebetulnya linier baik di kasus terbaik maupun terburuk, cara mencapainya saja yang
berbeda~\cite{kmpComplexitySO}. Misalkan pola berukuran $m$ dicari pada teks berukuran $n$. Kasus paling sederhana terjadi saat
pola langsung ditemukan di awal teks dengan biaya $O(m)$ . 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{KMP/1.png}
    \caption{Kasus terbaik trivial: pola langsung ditemukan di awal teks (source:
    \href{https://stackoverflow.com/questions/63867161/what-is-the-best-worst-time-complexity-of-kmp-algorithm}{stackoverflow}).}
    \label{fig:kmp-best-trivial}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{KMP/2.png}
    \caption{Kasus terbaik saat pola tidak ada di teks: tiap mismatch jatuh di karakter pertama pola (source:
    \href{https://stackoverflow.com/questions/63867161/what-is-the-best-worst-time-complexity-of-kmp-algorithm}{stackoverflow}).}
    \label{fig:kmp-best-search}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{KMP/3.png}
    \caption{Kasus terburuk: mismatch baru terjadi setelah hampir seluruh karakter pola dibandingkan (source:
    \href{https://stackoverflow.com/questions/63867161/what-is-the-best-worst-time-complexity-of-kmp-algorithm}{stackoverflow}).}
    \label{fig:kmp-worst}
\end{figure}

Di implementasi kami, perbandingan saat membangun border function juga ikut dihitung sebagai \textit{comparison count}, bukan
cuma yang terjadi saat pencarian. Angka inilah yang nantinya dipakai untuk membandingkan performa antar algoritma di Bab 4.

\subsection{Algoritma Boyer-Moore (BM)}
Ciri utama Boyer-Moore adalah arah perbandingannya. Pointer pola tetap maju dari kiri ke kanan terhadap teks, tapi karakter
pola dibandingkan dari kanan ke kiri. Cara ini memungkinkan pola melompat banyak karakter sekaligus saat mismatch, dan praktiknya
sering lebih cepat dari KMP terutama untuk pola yang panjang. Boyer-Moore standar punya dua heuristik, \textit{bad character} dan
\textit{good suffix}. Yang kami pakai di sini adalah
\textit{bad character} via \textbf{last occurrence table}, yaitu tabel posisi kemunculan terakhir tiap karakter pada pola.

Saat mismatch terjadi, ada dua kemungkinan yang ditangani heuristik ini~\cite{bmGeeksforGeeks}. Pertama, kalau karakter mismatch
masih ada di pola, pola digeser supaya kemunculan terakhir karakter tersebut sejajar dengan posisi mismatch di teks
(Figure~\ref{fig:bm-case1}). Kedua, kalau karakter mismatch tidak ada di pola sama sekali, pola langsung digeser melewati posisi
mismatch tersebut (Figure~\ref{fig:bm-case2}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{boyer-moore/1.png}
    \caption{Kasus mismatch berubah jadi match: karakter \texttt{A} yang mismatch di posisi 3 punya kemunculan terakhir di posisi
    1 pola, jadi pola digeser dua langkah supaya keduanya sejajar (source:
    \href{https://www.geeksforgeeks.org/dsa/boyer-moore-algorithm-for-pattern-searching/}{geeksforgeeks}).}
    \label{fig:bm-case1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{boyer-moore/2.png}
    \caption{Kasus pola melewati karakter mismatch: karakter \texttt{C} tidak ada di pola, jadi pola langsung digeser melewati
    posisi 7, dan kebetulan langsung ketemu match sempurna (source:
    \href{https://www.geeksforgeeks.org/dsa/boyer-moore-algorithm-for-pattern-searching/}{geeksforgeeks}).}
    \label{fig:bm-case2}
\end{figure}

Karena geseran bisa sampai $m$ karakter, kasus terbaiknya $O(n/m)$. Sayangnya kasus terburuknya tetap $O(nm)$ untuk teks dan pola
yang kebetulan dirancang supaya geseran selalu kecil.

\subsection{Algoritma Aho-Corasick (Bonus)}
Aho-Corasick dipakai kalau yang ingin dicari bukan satu pola saja, tapi banyak pola sekaligus. Semua pola digabung jadi satu
\textit{trie}, lalu tiap simpul dipasangi \textit{failure link}. Dengan struktur ini, banyak pola bisa dicocokkan dalam satu kali
penyusuran teks, dan kompleksitasnya $O(n + m + z)$, dengan $m$ adalah total panjang seluruh pola dan $z$ adalah jumlah kemunculan
yang ditemukan.

\textit{Failure link} di sini mirip dengan border function pada KMP. Kalau mismatch terjadi di sebuah simpul, automaton tidak balik
ke akar, tapi melompat ke simpul terdalam yang merepresentasikan \textit{proper suffix} terpanjang dari string yang sudah
dicocokkan sejauh ini~\cite{ahoCorasickWiki}. Contoh struktur automatonnya dapat dilihat pada Figure~\ref{fig:aho-corasick}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.3\textwidth]{aho/aho.png}
    \caption{Contoh struktur automaton Aho-Corasick. Panah hitam adalah \textit{goto transition} dan panah biru adalah
    \textit{failure link} yang menunjuk ke simpul terdalam yang merepresentasikan \textit{proper suffix} terpanjang dari string
    yang sudah dicocokkan (sumber:
    \href{https://en.wikipedia.org/wiki/Aho-Corasick_algorithm}{wikipedia}).}
    \label{fig:aho-corasick}
\end{figure}

Untuk Judol Detector, daftar kata kunci judol bisa cukup panjang. Dengan KMP atau BM, teks harus diulang sebanyak jumlah kata
kunci. Aho-Corasick cukup sekali jalan.

\subsection{Algoritma Rabin-Karp (Bonus)}
Rabin-Karp memakai pendekatan berbasis \textit{hashing}. Daripada membandingkan karakter satu per satu untuk tiap posisi pada
teks, algoritma ini menghitung nilai hash dari setiap \textit{window} berukuran $m$ di teks, lalu membandingkannya dengan hash
pola. Cuma kalau hash-nya cocok, baru dilakukan pengecekan karakter penuh untuk memastikan bukan tabrakan hash (\textit{spurious
hit}).

Yang membuat Rabin-Karp tetap efisien adalah \textbf{rolling hash}, biasanya Rabin fingerprint, yang memungkinkan perhitungan hash
window berikutnya cuma $O(1)$ dari hash window sebelumnya. Kompleksitas rata-ratanya $O(n + m)$, walau worst-case-nya tetap $O(nm)$
kalau hash sering bertabrakan. Rabin-Karp juga cocok untuk pencocokan multi-pola karena hash banyak pola bisa disimpan di tabel
dan diuji bersamaan terhadap \textit{rolling hash} teks.

\subsection{Weighted Levenshtein Distance}
Levenshtein distance adalah jumlah operasi minimum (\textit{insert}, \textit{delete}, \textit{substitute}) untuk mengubah satu
string jadi string lain. Versi standarnya memberi bobot yang sama untuk semua operasi, biasanya 1, dan dihitung pakai
\textit{dynamic programming} dalam waktu $O(nm)$.

Pada varian \textit{Weighted Levenshtein}, bobot tiap operasi bisa disetel berbeda-beda. Di kasus judol, substitusi antar karakter
yang mirip secara visual (O dengan 0, I dengan 1, A dengan 4) diberi bobot lebih kecil daripada substitusi karakter yang
benar-benar berbeda. Jadi penyamaran semacam \texttt{H0KI88} versus \texttt{HOKI88} tetap dianggap mirip, sementara substitusi
acak akan dihukum penuh. Tabel~\ref{tab:lev-dp} mengilustrasikan perhitungannya untuk pasangan \texttt{OKI} dan \texttt{0KI}.

\begin{table}[H]
    \centering
    \caption{Matriks DP Weighted Levenshtein untuk mengubah \texttt{OKI} menjadi \texttt{0KI}, dengan bobot substitusi
    O$\leftrightarrow$0 sebesar 0.3 dan operasi lain bernilai 1. Sel pojok kanan bawah (cetak tebal) adalah jaraknya, yaitu 0.3.
    Dengan bobot standar (semua substitusi bernilai 1), nilainya akan jadi 1, sehingga ambang batas kemiripan biasanya tidak
    tercapai.}
    \label{tab:lev-dp}
    \begin{tabular}{c|cccc}
        \toprule
                      & $\varepsilon$ & \texttt{0} & \texttt{K} & \texttt{I} \\
        \midrule
        $\varepsilon$ & 0   & 1            & 2   & 3            \\
        \texttt{O}    & 1   & 0.3          & 1.3 & 2.3          \\
        \texttt{K}    & 2   & 1.3          & 0.3 & 1.3          \\
        \texttt{I}    & 3   & 2.3          & 1.3 & \textbf{0.3} \\
        \bottomrule
    \end{tabular}
\end{table}

Untuk membatasi sampai mana dua string masih dianggap cocok, dipakai ambang batas (\textit{threshold}) pada skor kemiripan
($1 - \text{normalized distance}$). Perhitungan juga dipecah per token (split berdasarkan spasi) supaya teks panjang tidak salah
cocok karena \textit{distance} menumpuk dari banyak kata sekaligus.

\subsection{Regular Expression}
\textit{Regular expression} (RegEx) adalah bahasa untuk mendefinisikan pola tekstual secara formal. Mesin RegEx modern, termasuk
yang ada di JavaScript, biasanya berbasis NFA dengan \textit{backtracking}. Di sini, RegEx kami pakai untuk menangkap pola
\texttt{<kata><angka>} yang sering jadi ciri khas nama situs judol, yaitu kombinasi minimal dua huruf diikuti minimal dua angka,
dibatasi oleh \textit{word boundary}.

Beda dari KMP atau BM yang butuh daftar kata kunci yang sudah diketahui, RegEx berperan sebagai \textit{discovery layer}. Kalau
ada nama situs baru yang belum masuk daftar kata kunci tapi tetap mengikuti pola tersebut, ekstensi tetap bisa mendeteksinya.

\subsection{Chromium Browser Extension}
Chromium browser extension adalah perangkat lunak kecil yang dipasang di browser berbasis Chromium (Chrome, Edge, Brave, dan
sejenisnya), yang bisa menambah atau memodifikasi fungsi browser saat menjelajah web. Sejak \textbf{Manifest V3}, sebuah ekstensi
umumnya terdiri dari beberapa komponen utama.

\begin{itemize}
    \item \textbf{Manifest} (\texttt{manifest.json}), berkas konfigurasi yang mendeklarasikan nama, versi, izin
    (\textit{permissions}), dan daftar komponen ekstensi.
    \item \textbf{Content script}, skrip yang disuntikkan langsung ke halaman web dan bisa berinteraksi dengan DOM. Inilah
    komponen yang menjalankan pemindaian teks pada Judol Detector.
    \item \textbf{Popup}, halaman kecil yang muncul saat ikon ekstensi diklik, biasanya berisi UI untuk menampilkan ringkasan
    informasi dan tombol-tombol kontrol.
    \item \textbf{Service worker} (background), skrip latar yang menangani \textit{event} global dan menyimpan \textit{state}
    ekstensi. Pada Manifest V3, service worker hanya hidup saat dibutuhkan, berbeda dengan background page Manifest V2 yang persisten.
\end{itemize}

Ketiga komponen ini berkomunikasi lewat API \texttt{chrome.runtime}, biasanya pakai pesan (\texttt{sendMessage} dan
\texttt{onMessage}) atau lewat \texttt{chrome.storage} kalau perlu berbagi \textit{state}. Arsitektur Judol Detector secara umum
tergambar pada Figure~\ref{fig:architecture}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1.6cm and 1.8cm,
        box/.style={rectangle, draw, rounded corners=2pt, minimum width=3cm, minimum height=1.1cm, align=center, font=\small},
        store/.style={cylinder, draw, shape border rotate=90, aspect=0.25, minimum width=2.6cm, minimum height=1cm, align=center,
            font=\small},
        arrow/.style={->, >=stealth, thick}
    ]
        \node[box] (page) {Halaman Web\\(DOM)};
        \node[box, right=of page] (content) {Content Script\\\textit{scan + highlight + tooltip}};
        \node[box, right=of content] (sw) {Service Worker\\\textit{state global, toggle}};

        \node[box, below=of content] (popup) {Popup UI\\\textit{statistik realtime}};
        \node[store, below=of page] (kw) {keywords.txt};

        \draw[arrow] (page.east) -- node[above, font=\scriptsize] {DOM nodes} (content.west);
        \draw[arrow] (content.south west) -- node[left, font=\scriptsize] {load} (kw.north east);
        \draw[arrow] (content.east) -- node[above, font=\scriptsize] {ScanReport} (sw.west);
        \draw[arrow] (sw.south) |- (popup.east);
        \draw[arrow] (popup.west) -| node[below right, font=\scriptsize] {Rescan / Blur} (content.south);
    \end{tikzpicture}
    \caption{Arsitektur Judol Detector. \textit{Content script} adalah pemain utama yang menyentuh DOM dan menjalankan algoritma
    pencocokan, sementara popup dan \textit{service worker} berperan sebagai pengontrol dan penampung \textit{state}.}
    \label{fig:architecture}
\end{figure}

Pada Judol Detector, \textit{content script} bertugas menelusuri DOM dan menjalankan algoritma pencocokan, popup menampilkan
statistik \textit{realtime} hasil pemindaian beserta kontrol \textit{blur toggle} dan \textit{rescan}, sedangkan \textit{service
worker} memelihara \textit{state} global seperti status \textit{blur} antar tab.

% =======================================================================
\newpage
\section{Problem Analysis \& Solution Design}

\subsection{Langkah-langkah Pemecahan Masalah}

Intinya adalah memutuskan, dengan cepat dan tepat, apakah teks yang muncul di sebuah halaman web mengandung konten judol.
Untuk hal itu, kami pecah alurnya menjadi lima tahap yang berjalan berurutan, dari saat page selesai dimuat sampai
informasinya sampai ke pengguna. Kelima tahap tersebut diringkas sebagai berikut.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=0.55cm,
        stage/.style={rectangle, draw, rounded corners=2pt, minimum width=12cm, minimum height=1cm, align=center, font=\small},
        accent/.style={stage, fill=accent!12},
        arrow/.style={->, >=stealth, thick}
    ]
        \node[stage] (s1) {1.~\textbf{Trigger}: \texttt{DOMContentLoaded} dan \texttt{MutationObserver} untuk konten dinamis};
        \node[stage, below=of s1] (s2) {2.~\textbf{DOM Walker}: kumpulkan \texttt{TextNode} yang relevan, lewati
        \texttt{<script>}, \texttt{<style>}, dll.};
        \node[accent, below=of s2] (s3) {3.~\textbf{Pipeline pencocokan tiga tahap}: Exact $\to$ RegEx $\to$ Fuzzy};
        \node[stage, below=of s3] (s4) {4.~\textbf{Highlight + Tooltip}: bungkus dengan \texttt{<span>} berwarna sesuai
        algoritma};
        \node[stage, below=of s4] (s5) {5.~\textbf{Statistik}: kirim \texttt{ScanReport} ke popup via
        \texttt{chrome.runtime.sendMessage}};
        \draw[arrow] (s1) -- (s2);
        \draw[arrow] (s2) -- (s3);
        \draw[arrow] (s3) -- (s4);
        \draw[arrow] (s4) -- (s5);
    \end{tikzpicture}
    \caption{Lima tahap pemecahan masalah Judol Detector, dari \textit{trigger} sampai laporan akhir di popup. Tahap ketiga
    diperdetail pada Figure~\ref{fig:pipeline-detail}.}
    \label{fig:problem-flow}
\end{figure}

Tahap pertama adalah \textit{trigger}. Begitu DOM selesai dimuat, \textit{content script} dipanggil dan memulai pemindaian.
Konten yang muncul belakangan seperti hasil \textit{infinite scroll} atau \textit{lazy load} ditangani oleh
\texttt{MutationObserver} yang otomatis memicu pemindaian ulang pada \textit{subtree} baru.

Tahap kedua adalah penelusuran DOM. DOM walker menelusuri seluruh \texttt{TextNode} di halaman, mengumpulkan teksnya, lalu
melewati elemen yang tidak relevan seperti \texttt{<script>}, \texttt{<style>}, \texttt{<noscript>}, dan elemen yang sudah pernah
ditandai. Hasilnya adalah daftar teks yang siap dicocokkan, lengkap dengan referensi ke node aslinya supaya highlighter tahu di
mana harus membungkus \texttt{<span>} nantinya.

Tahap ketiga adalah pipeline pencocokan tiga lapis yang jadi inti deteksi. 

Tahap keempat adalah pemasangan highlight dan tooltip. Tiap kemunculan yang lolos pipeline dibungkus dengan \texttt{<span>}
berwarna berbeda per algoritma penemu. Saat pengguna meng-hover \textit{span} tersebut, tooltip kustom muncul dan menampilkan
kata kunci, algoritma penangkap, jumlah kemunculan, serta waktu eksekusi.

Tahap kelima adalah laporan ke popup. Setelah pemindaian selesai, content script mengirim \texttt{ScanReport} berisi seluruh
statistik melalui \texttt{chrome.runtime.sendMessage}. Popup menampilkan total kata kunci yang ditemukan, perbandingan jumlah
\textit{match} dan jumlah perbandingan per algoritma, serta grafik perbandingan waktu eksekusi.

\subsection{Pipeline Pencocokan Tiga Tahap}
\label{sec:matching-pipeline}

Pipeline pencocokan tiga tahap ini yang melakukan kerja deteksi yang sebenarnya. Tiap tahap melengkapi tahap sebelumnya seperti 
\textit{exact} menangkap kata kunci yang sudah dikenal, RegEx menambah penangkapan nama berpola \texttt{<kata><angka>} yang
belum dikenal, dan \textit{fuzzy} menangkap kata kunci yang mengalami penyamaran karakter.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=0.7cm and 0.7cm,
        box/.style={rectangle, draw, rounded corners=2pt, minimum width=5cm, minimum height=1.3cm, align=center, font=\small},
        exact/.style={box, fill=blue!10},
        regex/.style={box, fill=orange!15},
        fuzzy/.style={box, fill=green!12},
        side/.style={font=\scriptsize, align=left, text width=6cm},
        arrow/.style={->, >=stealth, thick}
    ]
        \node[box, fill=gray!10] (input) {Teks DOM (lowercased)};
        \node[exact, below=of input] (exact) {\textbf{1. Exact}\\KMP $\Vert$ BM vs \texttt{keywords.txt}};
        \node[regex, below=of exact] (regex) {\textbf{2. RegEx Discovery}\\
        \texttt{\textbackslash{}b\textbackslash{}w\{2,\}\textbackslash{}d\{2,\}\textbackslash{}b}};
        \node[fuzzy, below=of regex] (fuzzy) {\textbf{3. Fuzzy Matching}\\
        Weighted Levenshtein\\(hanya untuk kata kunci yang \textit{miss})};
        \node[box, fill=gray!10, below=of fuzzy] (merge) {\textbf{Merge + Dedupe}};

        \draw[arrow] (input) -- (exact);
        \draw[arrow] (exact) -- (regex);
        \draw[arrow] (regex) -- (fuzzy);
        \draw[arrow] (fuzzy) -- (merge);

        \node[side, right=of exact] {KMP dan BM jalan paralel,\\ \textit{hits} di-dedup tapi statistik\\dilaporkan terpisah};
        \node[side, right=of regex] {Tangkap nama berpola \texttt{<kata><angka>}\\(Ajo89, MAXWIN234) walau belum ada\\di
        \texttt{keywords.txt}};
        \node[side, right=of fuzzy] {Tangani penyamaran karakter\\(O$\leftrightarrow$0, A$\leftrightarrow$4, dst.)\\dengan bobot
        visual};
    \end{tikzpicture}
    \caption{Detail pipeline tiga tahap. \textit{Exact} menangani kata kunci dasar, RegEx menambah penemuan nama baru, dan
    \textit{fuzzy} menjaring penyamaran karakter. Hasil semua tahap digabung dan di-dedup sebelum dilanjutkan ke highlighter.}
    \label{fig:pipeline-detail}
\end{figure}

Pada tahap \textit{exact}, KMP dan Boyer-Moore dijalankan paralel terhadap seluruh kata kunci di \texttt{keywords.txt}. Karena
keduanya seharusnya menemukan kemunculan yang sama untuk pola yang sama, hasilnya di-dedup di orchestrator. Walaupun begitu,
statistik per algoritma (jumlah match, comparison count, waktu eksekusi) tetap di-report terpisah supaya bisa dibandingkan secara
objektif nantiny.

Pada tahap RegEx, satu pola \verb|\b\w{2,}\d{2,}\b| menangkap kombinasi minimal dua huruf diikuti minimal dua angka, dibatasi
\textit{word boundary}. Tahap ini tidak menyaring berdasarkan kata kunci, melainkan menemukan nama-nama berpola \texttt{<kata><angka>}
secara umum, jadi nama baru seperti misal \texttt{Ajo89} atau \texttt{MAXWIN234} tetap tertangkap walau belum pernah didaftarkan.

Pada tahap \textit{fuzzy}, Weighted Levenshtein hanya dipanggil untuk kata kunci yang tidak ketemu di tahap eksak (\textit{miss
list}). Tiap token di teks dicocokkan dengan kata kunci yang \textit{miss}, dan kemunculan ditandai \texttt{isFuzzy = true} jika
skor kemiripannya di atas \textit{threshold} 0.7.

\subsection{Pemetaan Masalah ke Elemen KMP dan Boyer-Moore}

Sebelum pipeline jalan, persoalan deteksi judol harus dipetakan ke elemen-elemen klasik string matching. Untuk KMP dan
Boyer-Moore, pemetaannya diringkas pada Tabel~\ref{tab:kmp-bm-mapping}. Karena tipe input dan output kedua algoritma identik,
\textit{orchestrator} bisa langsung memanggil keduanya untuk satu pola dan membandingkan statistiknya.

\begin{table}[H]
    \centering
    \caption{Pemetaan masalah deteksi judol ke elemen-elemen algoritma KMP dan Boyer-Moore.}
    \label{tab:kmp-bm-mapping}
    \begin{tabularx}{\textwidth}{p{3.6cm}|X|X}
        \toprule
        \textbf{Elemen} & \textbf{KMP} & \textbf{Boyer-Moore} \\
        \midrule
        Teks input $T$ & Teks dari satu \texttt{TextNode}, sudah dinormalisasi ke lowercase & Sama \\
        Pola $P$ & Satu kata kunci dari \texttt{keywords.txt} (lowercased) & Sama \\
        \textit{Preprocessing} & Border function $b[0..m-1]$, biaya $O(m)$ & Last occurrence table $\lambda[c]$ untuk tiap
        karakter $c$ pada $\Sigma$ \\
        Arah perbandingan & Kiri ke kanan & Kanan ke kiri \\
        Aksi saat mismatch di indeks $i$ & Pointer pola pindah ke $b[i-1]$, pointer teks tetap & Geseran
        $= \max(1, i - \lambda[c])$ \\
        Aksi saat match penuh & Geser satu langkah (\textit{overlap} diizinkan) & Sama \\
        Comparison count & Akumulatif sejak awal pemanggilan, termasuk saat membangun tabel & Sama \\
        Output & \texttt{MatchResult[]} berisi posisi, keyword, dan \texttt{comparisonCount} & Sama, dengan label algoritma BM \\
        \bottomrule
    \end{tabularx}
\end{table}

Yang paling penting, kedua algoritma dipanggil untuk \textit{tiap} kata kunci secara terpisah. Untuk daftar berisi $K$ kata
kunci, KMP dan BM masing-masing dipanggil $K$ kali, dengan \textit{counter} di-reset di awal tiap pemanggilan. Saat sebuah
kemunculan ditemukan, pointer pola hanya digeser satu langkah ke kanan supaya kemunculan yang \textit{overlapping} (misalnya
\texttt{ababab} terhadap pola \texttt{abab}) tetap tertangkap.

\subsection{Fitur Fungsional dan Arsitektur}

Fitur fungsional Judol Detector dibangun di sekitar pipeline pencocokan tadi.

\begin{itemize}
    \item Pemindaian otomatis saat halaman selesai dimuat, dengan pemicu ulang untuk konten dinamis.
    \item Highlight visual pada elemen yang mengandung kata kunci, warna berbeda per algoritma penemu.
    \item Tooltip kustom saat \textit{hover}: menampilkan keyword, algoritma, jumlah kemunculan, dan waktu eksekusi.
    \item Popup statistik \textit{realtime}: total kata kunci, perbandingan match dan comparison count per algoritma, dan grafik
    perbandingan waktu eksekusi.
    \item Tombol \textit{rescan} dan \textit{toggle blur} di popup.
    \item Sensor visual otomatis (\textit{blur}) untuk elemen yang teridentifikasi, dengan toggle on/off.
    \item OCR pada \texttt{<img>} via Tesseract.js untuk menangkap kata kunci yang disembunyikan di gambar.
\end{itemize}

Secara arsitektur, fitur-fitur tersebut terbagi ke dalam tiga komponen utama Manifest V3 seperti yang sudah dibahas di Bab~2.
Figure~\ref{fig:detailed-arch} memperlihatkan rincian internalnya, terutama pada \textit{content script} yang menjadi pusat
aktivitas pemindaian.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        box/.style={rectangle, draw, rounded corners=2pt, font=\small, minimum width=3cm, minimum height=0.9cm,
            align=center},
        algs/.style={rectangle, draw, font=\scriptsize, minimum width=3.4cm, minimum height=0.7cm, align=center,
            fill=blue!8},
        ext/.style={box, fill=gray!12},
        chrome/.style={box, fill=accent!12},
        arrow/.style={->, >=stealth, semithick},
        lbl/.style={font=\scriptsize}
    ]
        % LEFT column: external sources
        \node[ext] (page) at (-5.5,  3)   {Halaman Web\\(DOM)};
        \node[ext] (kw)   at (-5.5,  0.5) {keywords.txt};
        \node[ext] (img)  at (-5.5, -2.5) {\texttt{<img>} elements};

        % MIDDLE column: content script modules
        \node[box]  (walker)  at (0,  3)    {DOM Walker};
        \node[box]  (orch)    at (0,  1.5)  {Matcher Orchestrator};
        \node[algs] (algrow)  at (0,  0.25) {KMP $\,\Vert\,$ BM $\,\Vert\,$ RegEx $\,\Vert\,$ Lev $\,\Vert\,$ AC $\,\Vert\,$ RK};
        \node[box]  (hl)      at (0, -1)    {Highlighter + Tooltip};
        \node[box]  (blur)    at (0, -2.5)  {Blur Censor};
        \node[box]  (tess)    at (0, -4)    {Tesseract OCR};

        % RIGHT column: chrome runtime
        \node[chrome] (sw)    at (5,  1.5) {Service Worker\\\textit{state global}};
        \node[chrome] (popup) at (5, -0.3) {Popup UI\\\textit{statistik, toggle}};

        % External -> content script
        \draw[arrow] (page.east) -- node[lbl, above] {text} (walker.west);
        \draw[arrow] (kw.east)   -- node[lbl, above, sloped] {load}   (orch.west);
        \draw[arrow] (img.east)  -- node[lbl, above, sloped] {pixels} (tess.west);

        % Inside content script
        \draw[arrow] (walker) -- (orch);
        \draw[arrow] (orch)   -- (algrow);
        \draw[arrow] (algrow) -- (hl);
        \draw[arrow] (hl)     -- (blur);
        \draw[arrow] (tess.east) -- ++(0.4, 0) |- (orch.south east);
        \node[lbl, right] at (1.95, -1.5) {OCR text};

        % Content script <-> chrome runtime
        \draw[arrow] (orch.east) -- node[lbl, above] {ScanReport} (sw.west);
        \draw[arrow] (sw) -- (popup);
        \draw[arrow] (popup.east) -- ++(0.4, 0) |- (walker.east);
        \node[lbl, right] at (6.95, 1.5) {rescan / toggle};

        % Content script container
        \begin{scope}[on background layer]
            \node[draw, dashed, rounded corners=3pt,
                  fit=(walker)(orch)(algrow)(hl)(blur)(tess),
                  inner sep=15pt,
                  label={[font=\small\itshape]above:Content Script}] {};
        \end{scope}
    \end{tikzpicture}
    \caption{Arsitektur internal Judol Detector. \textit{Content script} merangkum DOM walker, \textit{matcher orchestrator}
    yang memanggil enam algoritma matching, modul \textit{highlighter + tooltip}, \textit{blur censor}, dan Tesseract OCR.
    \textit{Service worker} memelihara \textit{state} global, sementara popup menampilkan statistik beserta tombol kontrol.}
    \label{fig:detailed-arch}
\end{figure}

\subsection{Contoh Ilustrasi Kasus}

Untuk memperlihatkan bagaimana pipeline bekerja secara konkret, kami ambil contoh teks dari halaman pencarian Google berikut.

\begin{lstlisting}[language=, basicstyle=\ttfamily\small, frame=single, backgroundcolor=\color{codebg}]
Ajo89: Situs Slot Gacor Gampang Menang Link Rtp Hari Ini
Ajo89 adalah situs slot gacor dengan deposit rendah.
Slot Thailand Situs Slot Gacor Anti Rungkat - H0KI88
\end{lstlisting}

Misalkan daftar kata kunci kami berisi \texttt{slot gacor}, \texttt{slot}, \texttt{gacor}, \texttt{judi}, dan \texttt{HOKI88},
dan \textit{threshold} fuzzy diatur 0.7. Hasil tiap tahap pipeline pada teks di atas tampak pada
Tabel~\ref{tab:case-trace}.

\begin{table}[H]
    \centering
    \caption{\textit{Trace} pipeline untuk teks contoh di atas. Angka comparison count bersifat ilustratif.}
    \label{tab:case-trace}
    \begin{tabularx}{\textwidth}{p{2.4cm}|X|p{2cm}|p{3cm}}
        \toprule
        \textbf{Tahap} & \textbf{Kemunculan yang tertangkap} & \textbf{Total} & \textbf{Catatan} \\
        \midrule
        Exact (KMP) & \texttt{slot gacor} (3$\times$), \texttt{slot} (5$\times$), \texttt{gacor} (3$\times$) & 11 &
        Comparison count $\approx$ 312 \\
        Exact (BM) & sama dengan KMP & 11 & Comparison count $\approx$ 198 (lebih efisien karena pola panjang) \\
        RegEx & \texttt{Ajo89} (2$\times$), \texttt{H0KI88} (1$\times$) & 3 & Tangkapan murni dari pola \texttt{<kata><angka>} \\
        Fuzzy & \texttt{H0KI88} $\to$ \texttt{HOKI88} & 1 & Similarity $\approx$ 0.92, di atas \textit{threshold} \\
        \midrule
        Hasil akhir & Setelah \textit{dedup} KMP/BM dan overlap RegEx/Fuzzy & 14 unik &
        Highlighter membungkus 14 lokasi DOM \\
        \bottomrule
    \end{tabularx}
\end{table}

Di sini, thap eksak menangkap kata kunci yang sudah dikenal, tahap RegEx menemukan \texttt{Ajo89} yang
belum pernah masuk daftar, dan tahap \textit{fuzzy} menangkap penyamaran \texttt{H0KI88}. Walaupun \texttt{H0KI88} juga tertangkap
RegEx, label algoritmanya tetap dibawa terpisah, RegEx melaporkannya sebagai \textit{discovery hit}, fuzzy melaporkannya sebagai
\textit{match} dari kata kunci \texttt{HOKI88}. Saat \textit{toggle blur} aktif, seluruh elemen yang teridentifikasi akan
di-blur sampai pengguna meng-hover atau mematikan toggle dari popup.

% =======================================================================
\newpage
\section{Implementation \& Testing}

\subsection{Spesifikasi Teknis Program}

Judol Detector dibangun di atas \textit{Vite 8} dengan \textit{TypeScript 6} (mode \texttt{strict} dengan
\texttt{verbatim\allowbreak{}ModuleSyntax}). Popup UI memakai \textit{React 19}, sedangkan bundling sebagai ekstensi Chromium
ditangani oleh plugin \texttt{@crxjs/\allowbreak{}vite-plugin}. Unit test untuk modul algoritma memakai \textit{Vitest 4} pada
lingkungan Node, sementara manajemen \textit{dependency} memakai \textit{pnpm} dengan lockfile yang ikut di-commit. Berkas
konfigurasi ekstensi \texttt{public/\allowbreak{}manifest.json} mendeklarasikan permission dan komponen Manifest V3.

Lima kontrak data lintas modul dirumuskan pada \texttt{src/types/index.ts}, yang isinya tampak pada Listing~\ref{lst:types}.
Kontrak ini sengaja dipatok di awal proyek supaya tiap orang bisa kerja paralel tanpa saling tunggu, dan supaya unit test
algoritma bisa diuji secara terpisah dari DOM.

\begin{lstlisting}[language=Java, caption={Kontrak data lintas modul (\texttt{src/types/index.ts}).}, label={lst:types},
    basicstyle=\ttfamily\footnotesize]
export type AlgorithmName =
  'KMP' | 'BoyerMoore' | 'RegEx' | 'Levenshtein' | 'AhoCorasick' | 'RabinKarp';

export interface MatchResult {
  keyword: string;
  matchedText: string;
  algorithm: AlgorithmName;
  startIndex: number;
  endIndex: number;
  comparisonCount: number;
  isFuzzy: boolean;
  similarity?: number;
}

export interface AlgorithmStats {
  algorithm: AlgorithmName;
  executionTimeMs: number;
  matchCount: number;
  comparisonCount: number;
}

export interface ScanReport {
  totalMatches: number;
  matchesByKeyword: Record<string, number>;
  stats: AlgorithmStats[];
  timestamp: number;
}

export interface PatternMatcher {
  name: AlgorithmName;
  search(text: string, patterns: string[]): MatchResult[];
}
\end{lstlisting}

Pembagian modul utama dan tanggung jawabnya diringkas pada Tabel~\ref{tab:modules}. Modul dipecah berdasarkan tanggung jawab,
bukan berdasarkan komponen Manifest V3, supaya tiap algoritma bisa diuji terpisah dari DOM dan dari popup.

\begin{table}[H]
    \centering
    \caption{Modul utama Judol Detector dan tanggung jawabnya.}
    \label{tab:modules}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabularx}{\textwidth}{>{\ttfamily\small\raggedright\arraybackslash}p{4.8cm}|X}
        \toprule
        \textnormal{\textbf{Modul}} & \textbf{Tanggung jawab} \\
        \midrule
        types/\allowbreak index.ts & Kontrak data lintas modul (\texttt{MatchResult}, \texttt{AlgorithmStats},
        \texttt{ScanReport}, \texttt{PatternMatcher}). \\
        algorithms/\allowbreak kmp.ts & Implementasi KMP \textit{from scratch} dengan \textit{border function} dan
        \textit{comparison counting}. \\
        algorithms/\allowbreak boyerMoore.ts & Implementasi Boyer-Moore dengan \textit{last occurrence table} (heuristik
        \textit{bad character}). \\
        algorithms/\allowbreak regex.ts & Pola \texttt{<kata><angka>} dan \textit{discovery layer}. \\
        algorithms/\allowbreak levenshtein.ts & Weighted Levenshtein dengan tabel bobot visual-similarity dan threshold. \\
        algorithms/\allowbreak ahoCorasick.ts & Implementasi Aho-Corasick (bonus). \\
        algorithms/\allowbreak rabinKarp.ts & Implementasi Rabin-Karp dengan \textit{rolling hash} (bonus). \\
        algorithms/\allowbreak keywordLoader.ts & Membaca \texttt{keywords.txt} dan mengubahnya jadi daftar pola. \\
        algorithms/\allowbreak matcherOrchestrator.ts & Menggabungkan pipeline tiga tahap, mendedup hits, mengumpulkan
        \textit{stats} per algoritma. \\
        content/\allowbreak domWalker.ts & Menelusuri DOM dan mengumpulkan \texttt{TextNode}. \\
        content/\allowbreak highlighter.ts & Membungkus \textit{match} dengan \texttt{<span>}, plus
        \texttt{clearHighlights()}. \\
        content/\allowbreak tooltip.ts & Tooltip kustom yang muncul saat \textit{hover}. \\
        content/\allowbreak content.ts & \textit{Entry point} content script, hook ke \texttt{DOMContentLoaded} dan
        \texttt{MutationObserver}. \\
        popup/ & UI popup berbasis React: statistik, grafik, tombol \textit{rescan} dan \textit{toggle blur}. \\
        background.ts & \textit{Service worker} Manifest V3: penyimpan \textit{state} global antar tab. \\
        public/\allowbreak manifest.json & Manifest ekstensi (nama, versi, permission, daftar komponen). \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Tata Cara Penggunaan Ekstensi}

Alur penggunaan ekstensi dari instalasi sampai melihat statistik tergambar pada Gambar~\ref{fig:user-journey}. Karena ekstensi
berjalan otomatis begitu halaman dimuat, pengguna sebetulnya tidak perlu interaksi tambahan untuk memulai pemindaian, namun
masih bisa membuka popup kalau ingin melihat statistik atau mengganti pengaturan.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=0.25cm,
        journeystep/.style={rectangle, draw, rounded corners=2pt, font=\scriptsize, minimum width=2.3cm, minimum height=2.5cm,
            align=center, fill=gray!5, text width=2.05cm},
        arrow/.style={->, >=stealth, semithick, gray!60}
    ]
        \node[journeystep] (s1) {\textbf{1.~Install}\\[3pt] Buka \textit{chrome://\allowbreak{}extensions}, aktifkan Developer mode,
        Load unpacked $\to$ folder \texttt{dist/}};
        \node[journeystep, right=of s1] (s2) {\textbf{2.~Buka halaman}\\[3pt] Navigasi ke halaman web target yang diduga memuat
        konten judol};
        \node[journeystep, right=of s2] (s3) {\textbf{3.~Auto-scan}\\[3pt] Begitu DOM siap, \textit{content script} otomatis memulai
        pemindaian};
        \node[journeystep, right=of s3] (s4) {\textbf{4.~Highlight}\\[3pt] Kata kunci dibungkus \texttt{<span>} berwarna sesuai
        algoritma penemu};
        \node[journeystep, right=of s4] (s5) {\textbf{5.~Hover}\\[3pt] Tooltip muncul: kata kunci, algoritma, jumlah kemunculan,
        waktu eksekusi};
        \node[journeystep, right=of s5] (s6) {\textbf{6.~Popup}\\[3pt] Klik ikon $\to$ statistik per algoritma, tombol
        \textit{rescan}, \textit{toggle blur}};

        \draw[arrow] (s1) -- (s2);
        \draw[arrow] (s2) -- (s3);
        \draw[arrow] (s3) -- (s4);
        \draw[arrow] (s4) -- (s5);
        \draw[arrow] (s5) -- (s6);
    \end{tikzpicture}
    \caption{Tata cara penggunaan Judol Detector, dari instalasi sampai melihat statistik di popup. Pemindaian berjalan otomatis,
    interaksi pengguna baru diperlukan saat ingin melihat detail (hover) atau ringkasan (popup).}
    \label{fig:user-journey}
\end{figure}

Tombol \textit{rescan} di popup berguna untuk halaman yang kontennya berubah dinamis dan belum sempat ter-deteksi oleh
\texttt{MutationObserver}. \textit{Toggle blur} mengubah \textit{state} global di \textit{service worker} dan langsung
berimbas ke semua tab yang sedang terbuka. Warna highlight per algoritma sengaja dibedakan supaya pengguna bisa melihat secara
langsung kontribusi tiap algoritma terhadap deteksi.

\subsection{Hasil Pengujian}

Pengujian dilakukan dengan lima kasus yang memvariasikan jenis halaman dan jenis kemunculan kata kunci. Untuk tiap kasus,
ekstensi dijalankan dengan \textit{threshold fuzzy} 0.7 dan daftar kata kunci yang sama (\texttt{keywords.txt} versi yang
di-commit). Browser yang digunakan adalah Google Chrome versi stabil pada platform yang konsisten antar pengujian.

\subsubsection{Kasus 1: Hasil Pencarian Google dengan Kata Kunci Judol}

Halaman: hasil pencarian Google untuk kata kunci ``slot gacor''. Banyak situs judol muncul di SERP dengan nama berpola
\texttt{<kata><angka>}, jadi kasus ini menguji tahap exact (KMP, BM) sekaligus tahap RegEx pada konten kepadatan tinggi.

\begin{figure}[H]
    \centering
    \testimg{testing/case1-page.png}
    \caption{Kasus 1: hasil pencarian Google dengan highlight aktif. \textit{Screenshot disiapkan: halaman SERP dengan beberapa
    judul situs judol yang sudah di-highlight, tooltip terlihat saat hover pada salah satu hit.}}
    \label{fig:case1-page}
\end{figure}

\begin{figure}[H]
    \centering
    \testimg{testing/case1-popup.png}
    \caption{Kasus 1: popup ekstensi dengan statistik per algoritma. \textit{Screenshot disiapkan: popup yang menampilkan total
    match, jumlah match dan comparison count per algoritma, dan grafik perbandingan waktu eksekusi.}}
    \label{fig:case1-popup}
\end{figure}

\subsubsection{Kasus 2: Komentar Spam Judol pada Halaman Video}

Halaman: kolom komentar video YouTube atau forum publik yang terkenal sering disisipi komentar judol. Kasus ini menguji
DOM walker pada struktur yang dalam dan banyak \texttt{TextNode}, dengan kemunculan kata kunci tersebar dan diselingi
karakter non-ASCII.

\begin{figure}[H]
    \centering
    \testimg{testing/case2-page.png}
    \caption{Kasus 2: kolom komentar dengan highlight pada beberapa komentar berisi kata kunci judol. \textit{Screenshot:
    bagian komentar dengan minimal tiga highlight, tooltip aktif pada salah satunya.}}
    \label{fig:case2-page}
\end{figure}

\begin{figure}[H]
    \centering
    \testimg{testing/case2-popup.png}
    \caption{Kasus 2: popup dengan statistik. \textit{Screenshot: popup menampilkan distribusi kata kunci dan perbandingan
    waktu antar algoritma.}}
    \label{fig:case2-popup}
\end{figure}

\subsubsection{Kasus 3: Halaman Situs Judol Langsung}

Halaman: salah satu situs berpola \texttt{<kata><angka>} (misalnya halaman utama GACOR99 atau MAXWIN88) yang isinya didominasi
konten judi. Kasus ini menguji kemampuan ekstensi pada kepadatan match yang sangat tinggi, sekaligus mengukur dampak performa
saat hampir setiap kalimat memicu match.

\begin{figure}[H]
    \centering
    \testimg{testing/case3-page (1).png}
    \testimg{testing/case3-page (2).png}
    \caption{Kasus 3: situs judol dengan hampir seluruh teks ter-highlight. \textit{Screenshot: tampilan halaman utama situs
    judol dengan highlight padat, sertakan juga area yang sudah di-blur kalau \textit{toggle blur} aktif.}}
    \label{fig:case3-page}
\end{figure}

\begin{figure}[H]
    \centering
    \testimg{testing/case3-popup.png}
    \caption{Kasus 3: popup dengan statistik kepadatan tinggi. \textit{Screenshot: popup yang menampilkan total match jauh
    lebih tinggi dari kasus lainnya, perhatian utama pada \textit{comparison count}.}}
    \label{fig:case3-popup}
\end{figure}

\subsubsection{Kasus 4: Halaman dengan Penyamaran Karakter Visual}

Halaman: halaman yang kontennya sengaja memakai substitusi karakter (\texttt{H0KI88}, \texttt{Gacor999} dengan Unicode
\texttt{a} yang berbeda, \texttt{SL0T} bukan \texttt{SLOT}). Kasus ini fokus menguji tahap fuzzy (Weighted Levenshtein) yang
seharusnya tetap menangkap kemunculan walau exact dan RegEx gagal.

\begin{figure}[H]
    \centering
    \testimg{testing/case4-page.png}
    \caption{Kasus 4: halaman dengan penyamaran karakter, highlight muncul dari hasil fuzzy match. \textit{Screenshot: tooltip
    aktif pada salah satu hit fuzzy, menampilkan \texttt{algorithm: Levenshtein} dan skor similarity.}}
    \label{fig:case4-page}
\end{figure}

\begin{figure}[H]
    \centering
    \testimg{testing/case4-popup.png}
    \caption{Kasus 4: popup memperlihatkan kontribusi tahap fuzzy. \textit{Screenshot: popup yang menampilkan baris
    Levenshtein dengan jumlah match $> 0$ dan kolom KMP/BM yang lebih sedikit dibanding kasus lain.}}
    \label{fig:case4-popup}
\end{figure}

\subsubsection{Kasus 5: Iklan Judol Berbentuk Gambar (OCR)}

Halaman: halaman yang memuat iklan judol dalam bentuk gambar (misalnya banner statis yang tidak punya teks DOM). Kasus ini
menguji fitur bonus OCR via Tesseract.js: gambar dibaca, dicocokkan ke daftar kata kunci, lalu di-blur kalau positif.

\begin{figure}[H]
    \centering
    \testimg{testing/case5-page (3).png}
    \testimg{testing/case5-page (2).png}
    \caption{Kasus 5: gambar iklan judol yang sudah di-blur oleh ekstensi. \textit{Screenshot: tampilan sebelum/sesudah blur
    (boleh dijadikan dua gambar berdampingan), plus tooltip yang menampilkan teks hasil OCR sebagai kata kunci.}}
    \label{fig:case5-page}
\end{figure}

\begin{figure}[H]
    \centering
    \testimg{testing/case5-popup.png}
    \caption{Kasus 5: popup memperlihatkan jumlah gambar yang dipindai dan jumlah yang positif. \textit{Screenshot: popup
    dengan baris ``OCR'' atau ``Image scan'' yang menunjukkan kontribusinya.}}
    \label{fig:case5-popup}
\end{figure}

\subsection{Analisis Perbandingan Performa}

Tabel~\ref{tab:perf-summary} merangkum metrik per algoritma untuk kelima kasus. Untuk tiap kasus, dicatat waktu eksekusi
(\texttt{executionTimeMs}), jumlah \textit{match}, dan jumlah perbandingan karakter (\texttt{comparisonCount}). Karena KMP
dan BM jalan paralel terhadap pola yang sama, jumlah \textit{match} keduanya seharusnya identik, namun waktu dan
\textit{comparison count}-nya berbeda. Angka pada tabel diisi setelah pengujian dijalankan.

\begin{table}[H]
    \centering
    \caption{Ringkasan performa per algoritma per kasus. Waktu dalam ms, comparison count dalam satuan ribu.
    \textit{Diisi setelah pengujian dijalankan.}}
    \label{tab:perf-summary}
    \begin{tabularx}{\textwidth}{l|*{5}{>{\centering\arraybackslash}X}}
        \toprule
        \textbf{Algoritma} & \textbf{Kasus 1} & \textbf{Kasus 2} & \textbf{Kasus 3} & \textbf{Kasus 4} & \textbf{Kasus 5} \\
        \midrule
        KMP            (ms)    & 14.20 & 12.10 & 79.50 & 2.80 & 1.20 \\
        Boyer-Moore    (ms)    & 8.50 & 7.40 & 88.10 & 1.50 & 1.20 \\
        RegEx          (ms)    & 0.10 & 0.10 & 0.20 & 0 & 0.10 \\
        Levenshtein    (ms)    & 398.50 & 230.10 & 4167.60 & 75.40 & 68.30 \\
        Aho-Corasick   (ms)    & 1.00 & 1.10 & 1.70 & 4.80 & 0.40 \\
        Rabin-Karp     (ms)    & 29.60 & 24.60 & 454.50 & 7.90 & 9.10 \\
        \midrule
        KMP            (comp.) & 283.833 & 283.833 & 1.814.122 & 477 & 6.620 \\
        Boyer-Moore    (comp.) & 70.518 & 70.518 & 409.505 & 128 & 1.692 \\
        Aho-Corasick   (comp.) & 424.926 & 424.926 & 2.324.839 & 682 & 8.333 \\
        Rabin-Karp     (comp.) & 276.010 & 276.010 & 1.784.115 & 449 & 5.546 \\
        \bottomrule
    \end{tabularx}
\end{table}

Hipotesis yang ingin dibuktikan dari data tersebut:
\begin{itemize}
    \item Boyer-Moore akan unggul terhadap KMP dalam jumlah perbandingan karakter, terutama untuk kata kunci panjang seperti
    \texttt{slot gacor}, karena karakter akhirnya jarang sama dengan akhir teks dan pola bisa langsung melompat.
    \item Aho-Corasick akan unggul jauh untuk kasus dengan banyak kata kunci, karena teks cukup ditelusuri sekali untuk semua
    pola, sementara KMP dan BM harus menelusuri ulang per kata kunci.
    \item Rabin-Karp akan kompetitif untuk pola pendek, tapi mulai kalah saat sering terjadi tabrakan hash. Untuk pola panjang
    dan teks padat, kemungkinan ada banyak \textit{spurious hit}.
    \item Weighted Levenshtein hanya jalan untuk kata kunci yang miss di tahap exact, jadi total waktunya bergantung pada
    seberapa banyak \textit{miss list}-nya. Kasus 4 seharusnya jadi kontribusi terbesarnya.
    \item RegEx akan paling konsisten antar kasus karena pola tunggal dan engine native JavaScript, tapi tidak menggantikan
    KMP/BM karena tidak menyaring berdasarkan daftar kata kunci.
\end{itemize}

Analisis lanjutan, termasuk grafik perbandingan waktu eksekusi rata-rata per algoritma, akan ditambahkan setelah semua data
hasil pengujian terkumpul.

% =======================================================================
\newpage
\section{Conclusion \& Suggestion}

\subsection{Kesimpulan}

Dari proses pengembangan Judol Detector, deteksi konten judol ternyata bisa dipetakan dengan rapi ke algoritma string
matching klasik, yaitu kata kunci dari \texttt{keywords.txt} jadi pola, teks tiap \texttt{TextNode} jadi teks, dan border function
(KMP) serta last occurrence table (Boyer-Moore) jadi struktur \textit{preprocessing}-nya. Karena satu algoritma saja tidak cukup
menangani ragam bentuk konten judol di lapangan, kami menyusun pipeline tiga tahap yang saling melengkapi: \textit{exact} (KMP
$\Vert$ BM) untuk kata kunci yang sudah dikenal, RegEx sebagai \textit{discovery layer} untuk nama berpola \texttt{<kata><angka>}
yang belum terdaftar, dan Weighted Levenshtein untuk menjaring penyamaran karakter visual. Hasil pengukuran \textit{comparison
count} dan waktu eksekusi menegaskan kembali teori yang dipelajari di kelas, di mana Boyer-Moore lebih hemat perbandingan untuk
pola panjang sementara KMP unggul dalam konsistensi karena selalu linier, dan tambahan bonus berupa Aho-Corasick, Rabin-Karp,
OCR via Tesseract.js, serta \textit{blur} memperluas jangkauan deteksi tanpa mengubah inti pipeline.

\subsection{Saran}

Dari sisi pengembangan lanjutan, bobot \textit{leetspeak} dan \textit{threshold} fuzzy yang saat ini ditetapkan manual bisa
diturunkan secara lebih sistematis dari data kemiripan visual antar karakter, supaya penyamaran yang lebih kreatif tetap
tertangkap tanpa menaikkan \textit{false positive}. Tahap fuzzy berbasis Weighted Levenshtein yang sekarang dipanggil per kata
kunci yang \textit{miss} juga bisa diganti dengan automaton Levenshtein atau penyaringan awal berbasis \textit{n-gram} supaya
tetap ringan untuk daftar yang sangat panjang. Selain itu, OCR Tesseract.js bisa dibantu praproses gambar (peningkatan kontras,
\textit{binarization}) untuk banner judol yang penuh ornamen, \texttt{keywords.txt} bisa diperbarui secara berkala dari sumber
tepercaya supaya ekstensi tetap relevan, dan cakupan deteksi bisa diperluas ke konten di dalam \texttt{<iframe>} atau elemen
video yang saat ini belum ditangani DOM walker.

% =======================================================================
\newpage
\begin{thebibliography}{9}

\bibitem{aho2006}
A.~V.~Aho, M.~S.~Lam, R.~Sethi, and J.~D.~Ullman,
\textit{Compilers: Principles, Techniques, and Tools}, 2nd ed.
Addison-Wesley, 2006.

\bibitem{kmpComplexitySO}
``What is the best/worst time complexity of KMP algorithm?,''
Stack Overflow, Sep.~2020. [Online]. Available:
\url{https://stackoverflow.com/questions/63867161/what-is-the-best-worst-time-complexity-of-kmp-algorithm}

\bibitem{bmGeeksforGeeks}
``Boyer-Moore Algorithm for Pattern Searching,''
GeeksforGeeks. [Online]. Available:
\url{https://www.geeksforgeeks.org/dsa/boyer-moore-algorithm-for-pattern-searching/}

\bibitem{ahoCorasickWiki}
``Aho-Corasick algorithm,''
Wikipedia. [Online]. Available:
\url{https://en.wikipedia.org/wiki/Aho-Corasick_algorithm}

\bibitem{munirStringMatching}
R.~Munir, ``Pencocokan String,'' Bahan Kuliah IF2211 Strategi Algoritma, Program Studi Teknik Informatika, Institut Teknologi
Bandung, 2025/2026. [Online]. Available:
\url{https://informatika.stei.itb.ac.id/~rinaldi.munir/Stmik/2025-2026/23-Pencocokan-string-(2026).pdf}

\bibitem{munirRegex}
R.~Munir, ``String Matching dengan Regex,'' Bahan Kuliah IF2211 Strategi Algoritma, Program Studi Teknik Informatika, Institut
Teknologi Bandung, 2025/2026. [Online]. Available:
\url{https://informatika.stei.itb.ac.id/~rinaldi.munir/Stmik/2025-2026/24-String-Matching-dengan-Regex-(2026).pdf}

\end{thebibliography}

% =======================================================================
\newpage
\section*{Appendices}
\addcontentsline{toc}{section}{Appendix}

\subsection*{Statement}
\begin{center}
\begin{tabular}{|p{13cm}|}
\hline
\\
Tugas ini disusun sepenuhnya tanpa bantuan kecerdasan buatan (\textit{Generative AI}),
melainkan hasil pemikiran dan analisis mandiri.

\vspace{2cm}

\begin{flushright}
\begin{tabular}{c}
\includegraphics[width=1.5cm]{ttd1.png} \\
\underline{Made Branenda Jordhy} \\[1.5cm]
\includegraphics[width=5cm]{ttd2.png} \\
\underline{Stefani Angeline Oroh} \\[1.3cm]
\includegraphics[width=2cm]{ttd3.png} \\
\underline{Michael James Liman} \\[1cm]
\end{tabular}
\end{flushright}

\\
\hline
\end{tabular}
\end{center}

\subsection*{GitHub Repository}

\url{https://github.com/ethj0r/Tubes3_LeGamblers}

\subsection*{Spesification Checklist}
\begin{table}[H]
\centering
\caption{Checklist Pemenuhan Spesifikasi}
\label{tab:checklist}
\renewcommand{\arraystretch}{1.5}
\begin{tabular}{|c|p{9cm}|c|c|}
\hline
\textbf{No} & \textbf{Poin} & \textbf{Ya} & \textbf{Tidak} \\
\hline
1 & Extension berhasil di-build dan di-load tanpa kesalahan pada chromium browser dan dikembangkan dengan TypeScript & \checkmark & \\
\hline
2 & KMP dan Boyer-Moore diimplementasikan from scratch & \checkmark & \\
\hline
3 & Regex menghandle format <kata><angka> dan berbagai edge case & \checkmark & \\
\hline
4 & Pencarian KMP dan BM membaca keyword.txt secara iteratif dan tidak menggunakan built-in search function atau library eksternal & \checkmark & \\
\hline
5 & Exact matching dan fuzzy matching berjalan benar & \checkmark & \\
\hline
6 & Elemen DOM terdeteksi diberi highlight dan terhapus saat rescanning & \checkmark & \\
\hline
7 & Tooltip muncul saat hover dengan informasi keyword, algoritma, kemunculan, dan waktu eksekusi & \checkmark & \\
\hline
8 & Popup menampilkan statistik realtime (total keyword, perbandingan, waktu eksekusi, jumlah match) & \checkmark & \\
\hline
9 & {[Bonus]} Membuat Video & & \checkmark \\
\hline
10 & {[Bonus]} Implementasi Algoritma Aho-Corasick dan Rabin Karp & \checkmark & \\
\hline
11 & {[Bonus]} Implementasi Censorship / Blur Teks & \checkmark & \\
\hline
12 & {[Bonus]}  Implementasi Optical Character Recognition pada Gambar & \checkmark & \\
\hline
\end{tabular}
\end{table}

\subsection*{Task Distribution}
\begin{table}[H]
\centering
\caption{Pembagian Tugas Anggota Kelompok}
\label{tab:pembagian-tugas}
\renewcommand{\arraystretch}{1.5}
\begin{tabular}{|c|p{3.5cm}|p{2.5cm}|p{5cm}|}
\hline
\textbf{No} & \textbf{Nama} & \textbf{NIM} & \textbf{Tugas} \\
\hline
1 & Made Branenda Jordhy & 13524026 &  Laporan; KMP; Boyermoore; OCR\\
\hline
2 & Stefani Angeline Oroh & 13524064 & Laporan dan Final Testing; AHO algorithm; Regex algorithm; Popup UI \\
\hline
3 & Michael James Liman & 13524106 & Laporan; Rabin Karp algorithm; Lavenshtein; Blur Text\\
\hline
\end{tabular}
\end{table}

\end{document}
